version: '3.8'

services:
  # 1. SERVIÇO DE TUNELAMENTO SSH DEDICADO (Mantido, assumindo que é necessário para outro serviço)
  ssh_tunnel:
    build:
      context: .
      dockerfile: dockerfile.ssh
    container_name: ssh_tunnel_planka
    restart: unless-stopped
    # logging:
    #   driver: "none"

  planka:
    image: ghcr.io/plankanban/planka:2.0.0-rc.4
    restart: on-failure

    volumes:
      - favicons:/app/public/favicons
      - user-avatars:/app/public/user-avatars
      - background-images:/app/public/background-images
      - attachments:/app/private/attachments

    ports:
      - 7002:1337

    environment:
      - BASE_URL=http://ipelabor.sytes.net:7002

      # 🚀 MUDANÇA CRÍTICA: DATABASE_URL APONTANDO PARA O SERVIDOR EXTERNO
      # Formato: postgresql://user:password@host:port/database
      # Usando 'root', 'q1w2e3', 'ipelabor.sytes.net', porta '7432' e 'planka' (BD padrão)
      - DATABASE_URL=postgresql://root:q1w2e3@ipelabor.sytes.net:7432/planka

      - SECRET_KEY=8A5kLzTq9oPj1fGyC4hR2sXbN7vM3uW6eD0iFaE5gB9cJxK7lY8mZ4pV6rS3tQ2wU1

      # 📝 Configuração de administrador padrão do Planka (Recomendado para primeiro acesso)
      - DEFAULT_ADMIN_EMAIL=gustavo.souza@ipelabor.com.br
      - DEFAULT_ADMIN_PASSWORD=MagicKir137!
      - DEFAULT_ADMIN_NAME=Gustavo Souza
      - DEFAULT_ADMIN_USERNAME=twentyone
      # Outras configurações opcionais
      # - LOG_LEVEL=warn
      # ... (demais variáveis de ambiente comentadas)

    depends_on:
      ssh_tunnel:
        condition: service_started
      # 💥 DEPENDÊNCIA DO POSTGRES LOCAL REMOVIDA

      # Definição dos volumes para persistência de arquivos do Planka
volumes:
  favicons:
  user-avatars:
  background-images:
  attachments: # 💥 VOLUME 'db-data' REMOVIDO
